// MAX TIME v51 — cleaner exercise countdown layout
(function(){
  const clamp=v=>Math.max(10,Math.round((Number(v)||10)/10)*10);
  const findEx=id=>{for(let d=1;d<=5;d++){const ex=P[d]&&P[d].ex&&P[d].ex.find(x=>x.id===id);if(ex)return ex}return null};
  const sets=id=>{const ex=findEx(id);return ex?getSets(ex):null};
  window.mtExerciseTimerAdjust=function(id,i,delta){const a=sets(id),ex=findEx(id);if(!a||!a[i]||!ex)return;a[i].reps=String(clamp((Number(a[i].reps)||ex.min)+delta));save();render()};
  window.mtExerciseTimerToggle=function(id,i){const ex=findEx(id),a=sets(id);if(!ex||!a||!a[i])return;const s=a[i];if(S.timer&&S.timer.on&&S.timer.exerciseTimer&&S.timer.exerciseSetId===id&&S.timer.exerciseSetIndex===i){const left=clamp(S.timer.left||s.reps||ex.min);s.reps=String(left);window.stopTimer(true);save();render();return}const sec=clamp(Number(s.reps)||ex.min);s.reps=String(sec);save();window.startTimer(sec,ex.n,ex.id);S.timer.exerciseTimer=true;S.timer.exerciseSetId=id;S.timer.exerciseSetIndex=i;S.timer.exerciseConfigured=sec;render()};
  window.mtExerciseTimerDismiss=function(){try{window.stopTimer(true)}catch(e){S.timer={on:false,left:0,max:0,ex:'',eid:null,mod:false,saved:false};render()}};
  const baseOverlay=timerOverlay;
  timerOverlay=function(){let h=baseOverlay();if(S.timer&&S.timer.exerciseTimer){h=h.replace(/onclick="adjTimer\(-15\)"/g,'onclick="adjTimer(-10)"').replace(/onclick="adjTimer\(15\)"/g,'onclick="adjTimer(10)"').replace(/−15С/g,'−10С').replace(/\+15С/g,'+10С').replace('ОТДЫХ','ТАЙМЕР УПРАЖНЕНИЯ');h=h.replace('<button class="tmr-close" onclick="stopTimer()">✕</button>','<button class="tmr-close" onclick="mtExerciseTimerDismiss()">✕</button>').replace('<button class="skip-btn" onclick="stopTimer()">ПРОПУСТИТЬ →</button>','<button class="skip-btn" onclick="mtExerciseTimerDismiss()">ПРОПУСТИТЬ →</button>')}return h};
  const oldRT=renderTraining;
  renderTraining=function(){let h=oldRT(),wo=P[S.day];if(!wo)return h;for(const ex of wo.ex){if(!(ex.time&&ex.noLoad))continue;const a=getSets(ex),marker=`<div class="exname">${ex.n}`,pos=h.indexOf(marker);if(pos<0)continue;const cs=h.lastIndexOf('<div class="card">',pos),nc=h.indexOf('<div class="card">',pos+marker.length),ce=nc<0?h.length:nc;let card=h.slice(cs,ce);a.slice(0,ex.s).forEach((s,i)=>{const sec=clamp(Number(s.reps)||ex.min),running=!!(S.timer&&S.timer.on&&S.timer.exerciseTimer&&S.timer.exerciseSetId===ex.id&&S.timer.exerciseSetIndex===i),old=new RegExp(`<button class="set-time"[^>]*>[^<]*<\\/button>`),ctl=`<div class="exercise-timer-control"><button class="et-adj" onclick="mtExerciseTimerAdjust('${ex.id}',${i},-10)">−10</button><span class="et-time">${sec}<small>сек</small></span><button class="et-adj" onclick="mtExerciseTimerAdjust('${ex.id}',${i},10)">+10</button><button class="et-toggle ${running?'stop':'start'}" onclick="mtExerciseTimerToggle('${ex.id}',${i})">${running?'СТОП':'СТАРТ'}</button></div>`;card=card.replace(old,ctl)});h=h.slice(0,cs)+card+h.slice(ce)}return h};
  const st=document.createElement('style');st.textContent=`
  .exercise-timer-control{grid-column:2/4;display:grid;grid-template-columns:42px minmax(58px,1fr) 42px 68px;gap:6px;align-items:center;width:100%}
  .exercise-timer-control+input{display:none!important}
  .et-adj,.et-toggle{height:40px;border-radius:10px;border:1px solid var(--line);background:#10141a;font:800 10px Manrope,sans-serif}
  .et-adj{color:var(--a);font-size:11px}.et-time{height:40px;display:flex;align-items:baseline;justify-content:center;gap:3px;border:1px solid var(--line);border-radius:10px;background:#10141a;color:var(--txt);font-size:18px;font-weight:800;letter-spacing:-.3px}.et-time small{font-size:8px;color:var(--mut);font-weight:700;text-transform:uppercase}
  .et-toggle{padding:0 7px;background:linear-gradient(135deg,var(--a),#ff9e5d);border-color:var(--a);color:#111;box-shadow:0 5px 14px rgba(255,138,61,.15)}.et-toggle.stop{background:rgba(255,93,93,.10);color:#ff8d8d;border-color:rgba(255,93,93,.35);box-shadow:none}
  .set:has(.exercise-timer-control){grid-template-columns:28px 1fr 1fr 68px 40px;padding:12px 0}.set:has(.exercise-timer-control) .prev-set{grid-column:2/4;margin-top:-2px}
  @media(max-width:520px){.exercise-timer-control{grid-template-columns:38px minmax(52px,1fr) 38px 62px;gap:5px}.set:has(.exercise-timer-control){grid-template-columns:24px 1fr 1fr 62px 36px}.et-adj,.et-toggle,.et-time{height:38px}.et-toggle{font-size:9px}}
  `;document.head.appendChild(st);
})();